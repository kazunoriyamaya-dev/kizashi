# 運用 — 本番デプロイ前 最終チェックリスト

Phase 0–15 全完了。本番リリース前にこのチェックリストを **上から順に** 完了させること。
全項目が ✅ になれば本番デプロイ可。

## 1. コード品質 (ローカル / CI で実行)

| #   | 項目               | コマンド                                                            | 期待                                                                                                                                                                            |
| --- | ------------------ | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1 | Lint               | `pnpm lint`                                                         | ✔ No ESLint warnings or errors                                                                                                                                                  |
| 1.2 | フォーマット       | `pnpm format:check`                                                 | All files use Prettier code style!                                                                                                                                              |
| 1.3 | 型チェック         | `pnpm type-check`                                                   | 既存の Supabase relational join 由来の型 `never` エラーは next build で `typescript.ignoreBuildErrors:true` により build にだけ影響しないようにしている。本ステップは情報目的。 |
| 1.4 | 単体テスト         | `pnpm test`                                                         | Test Files 10 passed, Tests 62 passed                                                                                                                                           |
| 1.5 | E2E (任意)         | `pnpm test:e2e`                                                     | TC001–TC025 (Playwright)                                                                                                                                                        |
| 1.6 | 本番ビルド         | `pnpm build`                                                        | ✓ Compiled successfully                                                                                                                                                         |
| 1.7 | `console.log` 不在 | `grep -rn 'console\.log' src/ \| grep -v lib/logger`                | 0 件                                                                                                                                                                            |
| 1.8 | secrets リーク無し | `grep -rE 'sk_(test\|live)_\|whsec_[a-zA-Z0-9]{20}' src/ supabase/` | 0 件                                                                                                                                                                            |

## 2. データベース (Supabase)

| #    | 項目                                      | 確認方法                                                                                          |
| ---- | ----------------------------------------- | ------------------------------------------------------------------------------------------------- |
| 2.1  | 本番プロジェクト作成 (Pro / Tokyo / PITR) | Supabase Dashboard                                                                                |
| 2.2  | `supabase link --project-ref xxx`         | 設定済み                                                                                          |
| 2.3  | 全 migrations 適用                        | `supabase migration list` で 21 件 (`20260504..20260515000002`)                                   |
| 2.4  | `seed/production.sql` 適用                | system_settings / cancel_policies / tickets / blog categories の初期データ                        |
| 2.5  | RLS 全テーブル有効化                      | `select tablename, rowsecurity from pg_tables where schemaname='public'` で全 ROW SECURITY = true |
| 2.6  | RLS シナリオ確認                          | `docs/test/rls-tests.sql` を psql で実行し期待結果と一致                                          |
| 2.7  | 本番 admin 招待                           | Supabase Auth → Invite user → `raw_user_meta_data` に `{"role":"admin","name":"…"}` を設定        |
| 2.8  | dev seed admin 不在                       | `select email from auth.users where email like '%@kizashi.example.com'` で 0 件                   |
| 2.9  | Storage バケット作成                      | `instructor-avatars` (RLS), `marketing-assets` (公開読取)                                         |
| 2.10 | PITR 有効化                               | Supabase Dashboard → Database → Backups                                                           |

## 3. Vercel

| #   | 項目                                                                  |
| --- | --------------------------------------------------------------------- |
| 3.1 | GitHub 連携 + main ブランチで自動デプロイ                             |
| 3.2 | Build Command: `pnpm build`、Install: `pnpm install`                  |
| 3.3 | Production / Preview 環境変数すべて設定 (下記 4 章)                   |
| 3.4 | カスタムドメイン (`kizashi.example.com`) を Domains に追加 + SSL 自動 |
| 3.5 | Cron Jobs に 6 個登録されている (本書 § 7)                            |
| 3.6 | Functions Region: `hnd1` (Tokyo) を指定                               |

## 4. 環境変数 (Vercel Production)

### 必須

- `APP_URL = https://kizashi.example.com`
- `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `DATABASE_URL`
- Stripe (本番): `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` (`pk_live_…`), `STRIPE_SECRET_KEY` (`sk_live_…`), `STRIPE_WEBHOOK_SECRET`, `STRIPE_CONNECT_WEBHOOK_SECRET`, `STRIPE_CONNECT_CLIENT_ID`
- Google: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_MAPS_API_KEY`
- LINE: `LINE_CLIENT_ID`, `LINE_CLIENT_SECRET`, `LINE_CHANNEL_ACCESS_TOKEN`, `LINE_CHANNEL_SECRET`
- Resend: `RESEND_API_KEY`, `EMAIL_FROM`, `EMAIL_REPLY_TO`
- Web Push: `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`
- セキュリティ: `ENCRYPTION_KEY` (`openssl rand -base64 32`), `CRON_SECRET` (`openssl rand -hex 32`), `ADMIN_BOOTSTRAP_TOKEN`

### Phase 15 マーケ (任意。導入する施策のみ)

- `TWITTER_BEARER_TOKEN`
- `META_PAGE_ACCESS_TOKEN` / `META_PAGE_ID` / `META_IG_USER_ID`
- `TIKTOK_ACCESS_TOKEN` / `YOUTUBE_ACCESS_TOKEN`
- `META_AD_ACCESS_TOKEN` / `META_AD_ACCOUNT_ID`
- `GOOGLE_ADS_DEVELOPER_TOKEN` / `GOOGLE_ADS_CUSTOMER_ID`
- `TIKTOK_ADS_ACCESS_TOKEN`
- `AFFILIATE_WEBHOOK_SECRET` (`openssl rand -hex 32`)

## 5. 外部サービス設定

### 5.1 Stripe

- [ ] Webhook エンドポイント登録: `https://kizashi.example.com/api/stripe/webhook`
  - Events: `checkout.session.completed`, `payment_intent.succeeded`, `charge.refunded`, `charge.refund.updated`
  - Signing secret → `STRIPE_WEBHOOK_SECRET`
- [ ] Connect Webhook: `https://kizashi.example.com/api/stripe/connect-webhook`
  - Events: `account.updated`, `payout.created`, `payout.paid`, `payout.failed`
  - Signing secret → `STRIPE_CONNECT_WEBHOOK_SECRET`
- [ ] Connect Express を有効化

### 5.2 Google Cloud

- [ ] OAuth 2.0 Client (本番) を作成
- [ ] 承認済みリダイレクト URI:
  - `https://xxx.supabase.co/auth/v1/callback`
  - `https://kizashi.example.com/api/instructor/google-calendar/callback`
- [ ] 有効 API: Calendar API, Routes API, Maps JavaScript API
- [ ] API キーに HTTP リファラ / IP 制限

### 5.3 LINE

- [ ] LINE Login (Channel) を本番用に作成 → Callback URL: `https://kizashi.example.com/api/auth/line/callback`
- [ ] LINE Messaging API (公式 LINE) を作成 → Webhook URL: `https://kizashi.example.com/api/line/webhook`
- [ ] 友だち追加 URL を顧客向け案内に記載

### 5.4 Resend

- [ ] ドメイン `kizashi.example.com` を追加
- [ ] SPF / DKIM / DMARC の DNS レコードを設定 → Verified 状態
- [ ] `EMAIL_FROM=noreply@kizashi.example.com` で送信テスト

### 5.5 Web Push (VAPID)

- [ ] 本番用に `npx web-push generate-vapid-keys` で新規鍵を生成
- [ ] dev 鍵を流用していないこと

## 6. セキュリティ

| #   | 項目                                                                                    |
| --- | --------------------------------------------------------------------------------------- |
| 6.1 | `SUPABASE_SERVICE_ROLE_KEY` 使用箇所が `src/lib/supabase/admin.ts` のみ                 |
| 6.2 | OAuth refresh_token が AES-GCM 暗号化保存されている (`encryption_test.ts`)              |
| 6.3 | Stripe / Stripe Connect / LINE Webhook の署名検証が動作                                 |
| 6.4 | アフィリエイト postback Webhook が `AFFILIATE_WEBHOOK_SECRET` を `timingSafeEqual` 検証 |
| 6.5 | Cron は `CRON_SECRET` Bearer 検証                                                       |
| 6.6 | `next.config.mjs` のセキュリティヘッダ (X-Frame-Options DENY 等) 有効                   |
| 6.7 | middleware の PUBLIC_PATHS に公開パスのみ含まれる                                       |

## 7. Vercel Cron 6 ジョブ

| Path                                    | 周期                     |
| --------------------------------------- | ------------------------ |
| `/api/cron/dispatch-notifications`      | `*/5 * * * *`            |
| `/api/cron/notify-ticket-expiring`      | `0 9 * * *` (09:00 JST)  |
| `/api/cron/sweep-expired-tickets`       | `0 1 * * *` (01:00 JST)  |
| `/api/cron/notify-reservation-reminder` | `0 10 * * *` (10:00 JST) |
| `/api/cron/marketing-dispatch`          | `*/10 * * * *`           |
| `/api/cron/marketing-ad-sync`           | `0 4 * * *` (04:00 JST)  |

Vercel Cron は Authorization Bearer に `CRON_SECRET` を自動付与する。

## 8. スモークテスト (本番)

デプロイ完了後、`https://kizashi.example.com` で以下を実施:

1. [ ] トップページが表示される
2. [ ] `/admin/login` で本番 admin がログインできる
3. [ ] `/admin` ダッシュボードに 0 件の KPI が表示される
4. [ ] `/admin/marketing` ダッシュボードが表示される
5. [ ] テスト用の LP を作成 → `/lp/<slug>` が公開され、「無料体験レッスンを予約する」CTA が表示される
6. [ ] テスト用のブログ記事を作成 → `/blog/<slug>` が公開される
7. [ ] 顧客アカウントを `/login` から新規作成 → `/mypage/trial-reservation` から体験予約申込ができる
8. [ ] Stripe Webhook が `/api/stripe/webhook` で `200` を返している (Stripe Dashboard 上で確認)
9. [ ] Vercel Cron が初回実行されエラーが無い (`Logs` で `200 OK`)
10. [ ] 通知メール (`体験予約完了`) が `EMAIL_FROM` のドメインで届く

## 9. 監視・運用準備

- [ ] Slack `#kizashi-alerts` を作成、Vercel / Supabase / Stripe からの通知連携
- [ ] エラー監視 (Sentry / Logtail など) を導入 (将来)
- [ ] KUGEDOU 山谷氏 (一次対応) に本番 admin 招待を送付
- [ ] バックアップ手順 (`docs/運用_バックアップ.md`) を関係者に共有
- [ ] ロールバック手順 (`docs/運用_ロールバック.md`) を pre-prod でリハーサル済み

## 10. ステータス記録

このチェックリストを全て満たしたら、本ドキュメント最下部に以下を追記しコミットする:

```
## デプロイ実施記録
- 実施日: YYYY-MM-DD
- 実施者: 名前
- Vercel deployment URL: https://xxxx.vercel.app
- 本番ドメイン: https://kizashi.example.com
- 適用 migration 件数: 21
- スモークテスト結果: 10/10 PASS
```
